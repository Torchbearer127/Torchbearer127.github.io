<footer class="site-footer">
	<div class="site-footer__inner">
		<section class="footer-identity" aria-labelledby="footer-identity-title">
			<h2 id="footer-identity-title">
				<span class="footer-identity__mark" aria-hidden="true"></span>
				执炬人 / <span translate="no">Torchbearer127</span>
			</h2>
			<p>Agent systems, cyberspace security, and the intersections between them.</p>
			<a href="/about#research">Research focus <span aria-hidden="true">→</span></a>
		</section>

		<nav class="footer-group" aria-labelledby="footer-explore-title">
			<h2 id="footer-explore-title">Explore</h2>
			<ul>
				<li><a href="/writing">Writing</a></li>
				<li><a href="/notes">Notes</a></li>
				<li><a href="/projects">Projects</a></li>
				<li><a href="/about">About</a></li>
			</ul>
		</nav>

		<nav class="footer-group" aria-labelledby="footer-elsewhere-title">
			<h2 id="footer-elsewhere-title">Elsewhere</h2>
			<ul>
				<li><a href="https://github.com/Torchbearer127" rel="me">GitHub</a></li>
			</ul>
		</nav>
	</div>

	<div class="site-footer__legal">
		<p>© 2026 执炬人</p>
		<p>Built with Astro</p>
	</div>
</footer>

<style>
	.site-footer {
		border-top: 1px solid var(--color-border-subtle);
		color: var(--color-text-muted);
		font-size: 0.875rem;
	}

	.site-footer__inner,
	.site-footer__legal {
		width: min(100%, var(--layout-page-max));
		margin-inline: auto;
		padding-right: max(var(--layout-gutter), env(safe-area-inset-right));
		padding-left: max(var(--layout-gutter), env(safe-area-inset-left));
	}

	.site-footer__inner {
		padding-block: var(--space-12);
		display: grid;
		gap: var(--space-10);
	}

	.footer-identity {
		max-width: 24rem;
	}

	.footer-identity h2,
	.footer-group h2 {
		margin: 0 0 var(--space-4);
		font-size: 0.875rem;
		font-weight: 680;
		letter-spacing: 0.02em;
	}

	.footer-identity h2 {
		display: flex;
		align-items: center;
		gap: var(--space-2);
	}

	.footer-identity__mark {
		width: 0.375rem;
		height: 0.375rem;
		border-radius: 50%;
		background: var(--color-ember);
	}

	.footer-identity p {
		margin: 0 0 var(--space-3);
	}

	.footer-identity a,
	.footer-group a {
		min-height: 2rem;
		display: inline-flex;
		align-items: center;
		color: var(--color-accent);
	}

	.footer-group ul {
		margin: 0;
		padding: 0;
		display: grid;
		gap: var(--space-1);
		list-style: none;
	}

	.site-footer__legal {
		padding-block: var(--space-5) max(var(--space-5), env(safe-area-inset-bottom));
		border-top: 1px solid var(--color-border-subtle);
		display: flex;
		flex-direction: column;
		gap: var(--space-1);
		font-size: 0.8125rem;
	}

	.site-footer p {
		margin: 0;
	}

	@media (min-width: 40rem) {
		.site-footer__inner {
			grid-template-columns: minmax(0, 2fr) minmax(8rem, 1fr) minmax(8rem, 1fr);
			gap: clamp(2.5rem, 8vw, 6rem);
		}

		.site-footer__legal {
			flex-direction: row;
			justify-content: space-between;
			align-items: baseline;
		}
	}

	@media (hover: hover) {
		.footer-identity a:hover,
		.footer-group a:hover {
			color: var(--color-accent-hover);
		}
	}
</style>
