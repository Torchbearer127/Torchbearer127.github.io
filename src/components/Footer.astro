<footer class="site-footer">
	<div class="site-footer__inner">
		<p>© 2026 执炬人 · <span translate="no">Torchbearer127</span></p>
		<p class="footer-note">A quiet place for security, systems, and thought.</p>
	</div>
</footer>

<style>
	.site-footer {
		border-top: 1px solid var(--border);
		color: var(--text-muted);
		font-size: var(--text-sm);
	}

	.site-footer__inner {
		width: min(100%, var(--page-width));
		margin-inline: auto;
		padding: var(--space-8) max(var(--page-gutter), env(safe-area-inset-right))
			max(var(--space-8), env(safe-area-inset-bottom))
			max(var(--page-gutter), env(safe-area-inset-left));
		display: flex;
		flex-direction: column;
		gap: var(--space-2);
	}

	.site-footer p {
		margin: 0;
	}

	.footer-note {
		font-family: var(--font-display);
		font-style: italic;
	}

	@media (min-width: 48rem) {
		.site-footer__inner {
			flex-direction: row;
			justify-content: space-between;
			align-items: baseline;
		}
	}
</style>
